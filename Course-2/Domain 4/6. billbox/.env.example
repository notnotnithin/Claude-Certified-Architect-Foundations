# Copy this file to ".env" (exact name, no .example) and paste in your real key.
#
#   Windows cmd:   copy .env.example .env
#
# Get a key from https://console.anthropic.com/  ->  API Keys
# BillBox uses the Claude API from Part 1 onward, so you will need this key
# throughout Domain 4 (unlike Domain 3, where most demos used Claude Code).
#
# Never commit your real .env file - it is already listed in .gitignore.

ANTHROPIC_API_KEY=sk-ant-your-key-here
