# ScholarDesk Exploration Scratchpad

> This is a SCRATCHPAD - a file where Claude Code writes down key findings as it
> explores the codebase, so they survive even as the conversation gets long.
> In a long session a model can start "forgetting" specifics it found earlier and
> fall back on generic patterns. Writing findings here keeps them anchored.
>
> Start this file empty (or with the headings below) and ask Claude Code to fill
> it in as it explores. See DEMO-PROMPTS.md.

## What each module does
- (Claude fills this in: e.g. "ask.py - entry point; loads one source; calls _shared")
-

## How data flows
- (e.g. "cli question -> ask.py -> retrieval.load_source -> _shared.ask_claude")
-

## Key functions and where they live
- (e.g. "search_sources -> scholardesk/retrieval.py")
-

## Open questions / things to check later
-
